{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Publier') }}</button>
    <button class="btn">{{ button_label|default('Prendre une photo') }}</button>
{{ form_end(form) }}
